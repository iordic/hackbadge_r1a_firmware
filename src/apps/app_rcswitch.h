#ifndef APP_RCSWITCH_H_
#define APP_RCSWITCH_H_
// App RC-Switch: recibe/replay/guarda señales decodificadas (rc-switch) y
// permite enviar guardadas o un código manual. Toda la lógica es interna
// (máquina de estados por modos); solo se expone el App global.
#endif
